server:
  port: 8081
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
      force-request: true
      force-response: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel_system?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
    username: root
    password: Hl369369
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  mvc:
    charset: UTF-8
    
logging:
  level:
    com.hotelsystem: DEBUG

# JWT 配置：请在生产环境替换为强随机字符串
jwt:
  secret: ChangeThisSecretToAStrongOne
  expiration-ms: 3600000

# 初始管理员账号（启动时自动创建，如果不存在）
app:
  initial-admin:
    username: admin
    password: admin123

